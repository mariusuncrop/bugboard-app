services:
  bugboard:
    build: .
    ports:
      - '4000:4000'
    environment:
      PORT: 4000
      AUTH_SECRET: dev-only-secret-change-me
      # Leave this off for anything other than a throwaway demo.
      ENABLE_TEST_ENDPOINTS: 'true'
      STATS_DELAY_MS: '1200'
